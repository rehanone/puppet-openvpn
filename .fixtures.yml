fixtures:
  forge_modules:
    stdlib:
      repo: "puppetlabs/stdlib"
    apt:
      repo: "puppetlabs/apt"
  repositories:
    easyrsa:
      repo: 'https://github.com/rehanone/puppet-easyrsa.git'
      ref: '0.2.1'
  symlinks:
    openvpn: "#{source_dir}"
